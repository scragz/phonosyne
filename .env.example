# Phonosyne Environment Variables
# Copy this file to .env and fill in your actual values.
# The .env file is gitignored and should not be committed to version control.

# Required: OpenRouter API Key for LLM agent calls
OPENROUTER_API_KEY=YOUR_KEY_HERE

# Optional: Override default model selections (see phonosyne/settings.py for defaults)
# MODEL_DESIGNER="openrouter/some-other-model"
# MODEL_ANALYZER="openrouter/another-model"
# MODEL_COMPILER="openrouter/yet-another-model"

# Optional: Override default execution mode ("subprocess" or "inline")
# PHONOSYNE_EXECUTION_MODE="inline"

# Optional: Override default number of workers for parallel processing
# PHONOSYNE_WORKERS=4

# Optional: Disable ANSI colors in terminal output (set to any value, e.g., "1")
# NO_COLOR=1
